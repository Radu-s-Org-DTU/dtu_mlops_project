steps:
  - name: "gcr.io/cloud-builders/docker"
    id: "Build container image"
    args:
      [
        "build",
        ".",
        "-t",
        "europe-west1-docker.pkg.dev/$PROJECT_ID/course02476/02476build",
        "-f",
        "Dockerfile"
      ]
  - name: "gcr.io/cloud-builders/docker"
    id: "Push container image"
    args:
      ["push", "europe-west1-docker.pkg.dev/$PROJECT_ID/course02476/02476build"]

options:
  logging: CLOUD_LOGGING_ONLY
